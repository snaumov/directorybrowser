import React from 'react';

const Directory = (props) => {
    return (
        <div className="directory">
            {props.directoryName};
        </div>
    );
};

export default Directory;